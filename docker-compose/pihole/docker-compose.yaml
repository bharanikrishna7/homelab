# Docker Compose configuration for setting up PiHole container
# More info at https://github.com/pi-hole/docker-pi-hole/ and https://docs.pi-hole.net/

services:
  # PiHole container
  pihole:
    # container image
    image: pihole/pihole:latest
    # container name
    container_name: pihole
    # container host
    hostname: pihole

    # restart the container unless manually stopped
    restart: unless-stopped
    
    # ports to forward from host to container
    ports:
      # DNS Ports
      - "53:53/tcp"
      - "53:53/udp"
      # HTTP Port
      - "8080:80/tcp"
      # HTTPs Port
      - "8443:443/tcp"
      # Uncomment the below if using Pi-hole as your DHCP Server
      #- "67:67/udp"
      # Uncomment the line below if you are using Pi-hole as your NTP server
      #- "123:123/udp"
    
    # environment variables for container
    environment:
      # set the appropriate timezone for your location from
      # https://en.wikipedia.org/wiki/List_of_tz_database_time_zones, e.g:
      TZ: 'Asia/Kolkata'
      
      # set a password to access the web interface.
      # uncomment the following line to enable password, else there'll be no password
      FTLCONF_webserver_api_password: ''                      # no password
      #FTLCONF_webserver_api_password: ${WEBSERVER_PASSWORD}  # use password
      
      
      # if using Docker's default `bridge` network setting the dns listening mode should be set to 'ALL'
      FTLCONF_dns_listeningMode: 'ALL'
    
    # volume mapping from host to container
    volumes:
      # For persisting Pi-hole's databases and common configuration file
      - '/srv/pihole/etc:/etc/pihole'
      # Uncomment the below if you have custom dnsmasq config files that you want to persist. Not needed for most starting fresh with Pi-hole v6. If you're upgrading from v5 you and have used this directory before, you should keep it enabled for the first v6 container start to allow for a complete migration. It can be removed afterwards. Needs environment variable FTLCONF_misc_etc_dnsmasq_d: 'true'
      #- './etc-dnsmasq.d:/etc/dnsmasq.d'

    # container labels
    labels:
      - 'admins.chekuri.org=true'

    # deployment (docker compose up) settings
    deploy:
      # set resource limits
      resources:
        # maximum resource limits
        limits:
          cpus: '0.1'
          memory: 1024m
        # minimum resource limits
        reservations:
          cpus: '0.05'
          memory: 32m

  # oauth2-proxy container
  pihole-oauth2-proxy:
    # container image
    image: quay.io/oauth2-proxy/oauth2-proxy:latest
    # container name
    container_name: pihole-oauth2-proxy
    # container hostname
    hostname: ${HOSTNAME}
    
    # restart the container unless manually stopped
    restart: unless-stopped
    
    # command to run on container start
    command: --config /oauth2-proxy.cfg
    
    # volume mapping from host to container
    volumes:
      # oauth application configuration
      - "./oauth2-proxy.cfg:/oauth2-proxy.cfg"
    
    # ports to be forwarded from host to container
    ports:
      # forwarding oauth2-proxy port to host
      - 8000:4180

    # container labels
    labels:
      - 'admins.chekuri.org=true'

    # container dependencies
    depends_on:
      # depends on pihole container
      pihole:
        # container must be healthy
        condition: service_healthy
    
    # deployment (docker compose up) settings
    deploy:
      # set resource limits
      resources:
        # maximum resource limits
        limits:
          cpus: '0.05'
          memory: 128m
        # minimum resource limits
        reservations:
          cpus: '0.01'
          memory: 32m