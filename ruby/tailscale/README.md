# Tailscale Docker Compose
Simple Tailscale docker compose to quickly setup and run tailscale as a docker container.

## How to run 
```shell
AUTHKEY=tskey-auth-<actual_auth_key> HOST=your_host_name docker compose up
```